#ifndef _BST_
#define _BST_

//- Functions -//

// Searching

int search(pTree, field_type);

// Insertion

pTree bstInsertion(pTree, pField);

// Deletion 

pTree deleteMax(pTree, field_type*);

#endif